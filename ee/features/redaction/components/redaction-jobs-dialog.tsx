/**
 * [self-host] Reconstructed module.
 *
 * Papermark's public repo imports this path but ships it only in their private
 * enterprise repo, so upstream `main` does not compile. PDF redaction is enterprise-only.
 */
export function RedactionJobsDialog(_props: any) {
  return null;
}
