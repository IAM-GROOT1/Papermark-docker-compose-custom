/**
 * [self-host] Reconstructed module.
 *
 * Papermark's public repo imports this path but ships it only in their private
 * enterprise repo, so upstream `main` does not compile. Custom link-preview editing is an enterprise feature. The underlying fields still exist on Brand, they just have no editor here.
 */
export function BrandingLinkPreviewForm(_props: any) {
  return null;
}
