/**
 * [self-host] Reconstructed module.
 *
 * Papermark's public repo imports this path but ships it only in their private
 * enterprise repo, so upstream `main` does not compile. The visual layout-preset picker is enterprise-only. Presets can still be applied through the individual layout controls.
 */
export function DataroomLayoutPresetCards(_props: any) {
  return null;
}
