/**
 * [self-host] Reconstructed module.
 *
 * Papermark's public repo imports this path but ships it only in their private
 * enterprise repo, so upstream `main` does not compile. Visitor-language selection is enterprise-only.
 */
export function VisitorLanguageCard(_props: any) {
  return null;
}
