/**
 * [self-host] Reconstructed module.
 *
 * Papermark's public repo imports this path but ships it only in their private
 * enterprise repo, so upstream `main` does not compile. Decorative browser chrome around the branding preview. Renders its children unwrapped so the preview itself still shows.
 */
import { ReactNode } from "react";

export function BrandingPreviewChrome({ children }: { children?: ReactNode }) {
  return <>{children}</>;
}
