/**
 * [self-host] Reconstructed module.
 *
 * Papermark's public repo imports this path but ships it only in their private
 * enterprise repo, so upstream `main` does not compile. The banner upload/edit control on the branding page is an enterprise feature; it renders nothing here.
 */
export function BannerEditor(_props: any) {
  return null;
}
