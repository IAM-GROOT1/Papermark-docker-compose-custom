/**
 * [self-host] Reconstructed module.
 *
 * Papermark's public repo imports this path but ships it only in their private
 * enterprise repo, so upstream `main` does not compile. Read-only social preview card; enterprise-only.
 */
export function BrandingSocialPreviewReadonly(_props: any) {
  return null;
}
