/**
 * [self-host] Reconstructed module.
 *
 * Papermark's public repo imports this path but ships it only in their private
 * enterprise repo, so upstream `main` does not compile. The confidential-view watermark/screenshot-guard overlay is enterprise-only, so nothing is drawn over the document.
 */
export function ConfidentialViewOverlay(_props: any) {
  return null;
}
