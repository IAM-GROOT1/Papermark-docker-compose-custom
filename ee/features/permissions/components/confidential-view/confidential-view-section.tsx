/**
 * [self-host] Reconstructed module.
 *
 * Papermark's public repo imports this path but ships it only in their private
 * enterprise repo, so upstream `main` does not compile. The link-settings section for confidential view. Absent here, so the option simply does not appear when configuring a link.
 */
export default function ConfidentialViewSection(_props: any) {
  return null;
}
