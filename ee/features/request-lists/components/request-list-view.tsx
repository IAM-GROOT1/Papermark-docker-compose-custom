/**
 * [self-host] Reconstructed module.
 *
 * Papermark's public repo imports this path but ships it only in their private
 * enterprise repo, so upstream `main` does not compile. Dataroom request lists are enterprise-only.
 */
export function RequestListView(_props: any) {
  return null;
}
