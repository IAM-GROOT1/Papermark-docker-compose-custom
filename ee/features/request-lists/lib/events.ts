/**
 * [self-host] Reconstructed module.
 *
 * Papermark's public repo imports this path but ships it only in their private
 * enterprise repo, so upstream `main` does not compile. Custom DOM event name used to toggle the viewer's request list.
 */
export const VIEWER_TOGGLE_REQUEST_LIST_EVENT =
  "papermark:viewer-toggle-request-list";
