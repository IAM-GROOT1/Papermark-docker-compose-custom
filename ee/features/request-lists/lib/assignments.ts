/**
 * [self-host] Reconstructed module.
 *
 * Papermark's public repo imports this path but ships it only in their private
 * enterprise repo, so upstream `main` does not compile. Whether a viewer is assigned to a request-list item, used to authorise viewer-side uploads. With request lists unavailable there are no assignments, so this is always false.
 */
export async function isViewerAssigned(..._args: any[]): Promise<boolean> {
  return false;
}
